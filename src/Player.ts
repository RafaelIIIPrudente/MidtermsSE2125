export class Player {
  constructor(public firstName: string, public lastName: string, public bBallNumber: string, public age: number) {
    this.firstName = firstName,
    this.lastName = lastName, 
    this.bBallNumber = bBallNumber,
    this.age = age
  }

  
}